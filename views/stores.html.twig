<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Stores</title>
    </head>
    <body>
        {% if stores is not empty%}
            <h1>Stores</h1>
            <ul>
            {% for store in stores %}
               <a href='/stores/{{store.getId}}'><li> {{store.getName}}</li></a>
            {% endfor %}
            </ul>
            <form action="/clearstores" method="post">
                <button type="submit" name="button">Delete all stores</button>
            </form>
        {% else %}
           <h1>Stores</h1>
           <p>There are no stores in the system yet</p>
        {% endif %}
       <form action="/stores" method="post">
           <label>Add a store</label>
           <input type="text" name="store_input" value="">
           <button type="submit" name="button">Add a store</button>
       </form>
       <a href='/'><button type="button" name="button">Home</button></a>
    </body>
</html>
