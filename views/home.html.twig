<html>
  <head>
    <meta charset="utf-8">
    <title>Shoe Finder</title>
  </head>
  <body>
    {% if brands is not empty%}
        <h1>Brands</h1>
        <a href='/addbrands'><button type="button" name="button">Add a brand</button></a>
        {% for brand in brands %}
            <a href='/brands/{{brand.getId}}'><li> {{brand.getName}}</li></a>
        {% endfor %}
    {% else %}
        <h1>Brands</h1>
        <p>There are no brands in the system yet</p>
        <a href='/addbrands'><button type="button" name="button">Add a brand</button></a>
        </form>
    {% endif %}
    {% if stores is not empty%}
        <h1>Stores</h1>
        <a href='/addstores'><button type="button" name="button">Add a store</button></a>
        {% for store in stores %}
            <a href='/stores/{{store.getId}}'><li> {{store.getName}}</li></a>
        {% endfor %}
    {% else %}
        <h1>Stores</h1>
        <p>There are no stores in the system yet</p>
        <a href='/addstores'><button type="button" name="button">Add a store</button></a>
    {% endif %}
    <form action="/" method="post">
        <button type="submit" name="button">Clear all</button>
    </form>
  </body>
</html>
